# HL 探索账本（单标的择时）

固定评估口径：`510300.XSHG`，腾讯前复权日线，2018-01-01 ~ 2026-02-28（约 8 年，1976 个交易日），初始资金 10 万，聚宽口径成本，`score = Sortino`。**只改 `example/strategy.py`，评估器冻结。**

笔数下限（本标的/区间规模）：**≥ 30**（单标的 8 年低频择时，基线 57 笔，从默认 50 下调后固定）。其余门槛沿用 skill 默认：Sortino 严格高于基线（硬门槛），总收益/年化/Sharpe ≥ 基线−5%，最大回撤 ≤ 基线+2pp，胜率 ≥ 基线−3pp。

## ⚠️ 评估器口径升级（2026-07，用户一次性授权改固定评估器）

聚宽实测发现本地 holdout 与聚宽差异达 8pp（本地 +20.62% vs 聚宽 +12.28%），根因是本地「当日收盘成交」的乐观偏差（详见 `docs/guide/common-pitfalls.md`）。经授权对 `backtest.py` 做**一次性口径对齐**，此后冻结：

1. **成交时点**：当日收盘算信号 → **次日开盘成交**（吃不到隔夜跳空）；
2. **整手**：买入按 100 股向下取整；
3. **滑点**：聚宽默认 `PriceRelatedSlippage(0.00246)`，买入价上浮/卖出价下浮各半；
4. **T+1**：次日开盘成交下「买入当日不可卖」天然满足。

价格与分红口径仍用腾讯前复权（分红按比例再投资隐含在前复权价里），与聚宽「真实价+现金分红」存在二阶残差，属可接受权衡。

**对齐效果**：新口径下同段本地 holdout 总收益 **+12.52%**，与聚宽 **+12.28%** 基本吻合（差 0.24pp），8pp 缺口消除。

**分数重锚（新口径，全窗口）**：以下 Round 2 及旧「基线」表格均为**旧口径（收盘成交）**，仅存档；新口径下重新测得——

| 策略 | 旧口径 score | 新口径 score | 新口径总收益 | 新口径回撤 | 笔数 |
| --- | --- | --- | --- | --- | --- |
| 纯 10/20 双均线（原始 demo） | −0.0240 | **−0.1755** | −27.11% | 42.34% | 57 |
| + 200 日趋势过滤（当前 strategy.py） | 0.1711 | **+0.0609** | +0.30% | 35.37% | 39 |

**结论**：趋势过滤在去掉收盘成交乐观偏差后**仍严格优于**纯双均线（−0.1755 → +0.0609），Round 2 的经济结论成立，不是乐观撮合的假象。新冻结基线锚点 = 纯 10/20 的 **−0.1755**；后续 HL 轮次以此为准。

## 基线（旧口径，存档 —— 下方 Round 2 表格同为旧口径）

| score | 总收益 | 年化 | Sharpe | 最大回撤 | 胜率 | 笔数 |
| --- | --- | --- | --- | --- | --- | --- |
| −0.0240 | −12.25% | −1.65% | −0.024 | 37.47% | 40.35% | 57 |

策略：10/20 双均线金叉满仓 / 死叉空仓。

## 轮次记录

### Round 2 — 200 日牛熊趋势过滤 · ACCEPT

- **Diagnose**：纯双均线无牛熊结构判断，在 2018 熊市、2022~2024 下行段里假金叉逆势做多被套，是净亏与 37% 回撤的主因（57 笔胜率仅 40%）。
- **Propose（经济含义）**：价格站上 200 日线（多头结构）才允许金叉做多；跌破 200 日线（空头结构）强制空仓。200 日线为经典牛熊分界，专治熊市逆势做多。结构性过滤，非连续变量窄坑。
- **Patch**：`strategy.py` 新增 `TREND_WINDOW=200`，`LOOKBACK=max(LONG_WINDOW, TREND_WINDOW)`，`decide` 加 `price > ma_trend` 做多前置条件、`price < ma_trend` 触发空仓。
- **Evaluate**：

  | 指标 | 基线 | 候选 | 门槛 | 判定 |
  | --- | --- | --- | --- | --- |
  | score (Sortino) | −0.0240 | **0.1711** | 严格 > 基线 | ✅ |
  | 总收益 | −12.25% | +16.08% | ≥ −17.25% | ✅ |
  | 年化 | −1.65% | +1.92% | ≥ −6.65% | ✅ |
  | Sharpe | −0.024 | 0.206 | — | ✅ |
  | 最大回撤 | 37.47% | 25.91% | ≤ 39.47% | ✅ |
  | 胜率 | 40.35% | 41.03% | ≥ 37.35% | ✅ |
  | 笔数 | 57 | 39 | ≥ 30 | ✅ |

- **Replay（holdout run-once，方向性）**：样本内 score −0.247→0.012（大亏→打平）；holdout 0.854→0.825（牛市段几乎无代价）；`train − holdout score = −0.81`（验证段更好），无过拟合迹象。
- **Decide**：**ACCEPT**。亏损转盈利、回撤大降、全门槛通过、样本内外方向一致。
- **Compress**：趋势过滤为唯一新增规则，无冗余。

---

# 横截面 v2 评估器（沪深300 / 月度 Top3 / IR）

固定评估口径：`universe_csi300.py` 固定快照（2026-07-05，300 只），2018-01-01 ~
2026-02-28（1976 交易日），初始 10 万，次日开盘/整手/滑点/成本，`score = IR`（相对
sh000300 超额的信息比率）。**只改 `example/strategy_xs.py`，`backtest_xs.py` 冻结。**

设计文档：`docs/design/cross-sectional-pipeline.md`（两级验收门槛：迭代宽进 / 部署严出）。

## v2 基础设施交付（2026-07-05，feat/xs-pipeline）

- **状态**：评估器代码闭环（Task 0–9），harness 9 项 pytest + py_compile + 双 export
  --check 全绿。
- **策略基线**：12-1 横截面动量（`LOOKBACK=252`, `SKIP=21`），等权 Top3，月度调仓。
- **聚宽终验**：`example/jq_strategy_xs_export.py`（`get_index_stocks` point-in-time 成分）。

## load_panel 数据层改进（2026-07-05）

腾讯 kline 批量拉取易 HTTP 501，`backtest_xs.py` 新增（基础设施，不改评估撮合口径）：

1. **探测 + 快速降级**：启动时探一次腾讯；501 则本会话全量走新浪（避免 300×退避重试）。
2. **增量缓存**：`*_partial.pkl` 每拉完一只即落盘，中断可续拉。
3. **新浪备用源**：腾讯不可用时降级（**不复权**；主路径仍为腾讯**前复权**）。

详见 `docs/guide/common-pitfalls.md` §3。

## 基线锚点 — 12-1 动量 · v0（2026-07-05 登记）

**数据说明**：本次登记时腾讯接口 501，面板经**新浪不复权**拉取并缓存（
`example/.cache/panel_csi300_2026-07-05_2018-01-01_2026-02-28.pkl`）。动量因子在
高股息除权日有二阶偏差；腾讯恢复后应删缓存重拉前复权面板再对比。聚宽终验仍为准。

**两级门槛锚定**（基线 IR = **+0.4436**）：

| 门槛 | 判据 | 基线实测 |
| --- | --- | --- |
| 迭代·IR | 全窗口 IR 严格 > 基线 | **+0.4436**（锚点） |
| 迭代·holdout 不塌 | holdout IR ≥ 0 且 样本内−holdout ≤ 1.0 | +0.8774 ✅；分差 **−0.5461** ✅ |
| 迭代·不靠单点 | 单只×单期贡献 ≤ 30% | 全窗口 5.9% ✅；holdout **22.7%** ✅ |
| 迭代·广度 | 有效持仓次数 ≥ 100 | **255** ✅ |
| 部署·t 显著 | 全窗口 t ≥ 2（≈ IR ≥ 0.70） | t = **+1.24** ❌ |
| 部署·样本外为正 | holdout IR > 0 | **+0.8774** ✅ |

### 全窗口（2018-01-01 ~ 2026-02-28）

| 指标 | 值 |
| --- | --- |
| **IR (score)** | **+0.4436** |
| 年化超额 | +16.45% |
| 超额最大回撤 | 60.28% |
| 月度胜率 | 53.6% |
| 超额 t 统计量 | +1.24 |
| 年化换手（单边） | 8.57× |
| 有效持仓次数 | 255 |
| 单点最大贡献 | 5.9% |

### 样本内（2018-01-02 ~ 2024-07-08，约 80%）

| IR | 年化超额 | 超额最大回撤 | 月度胜率 | t | 有效持仓 | 单点贡献 |
| --- | --- | --- | --- | --- | --- | --- |
| +0.3313 | +12.16% | 60.28% | 52.6% | +0.83 | 198 | 4.4% |

### holdout（2024-07-09 ~ 2026-02-27，约 20%）

| IR | 年化超额 | 超额最大回撤 | 月度胜率 | t | 有效持仓 | 单点贡献 |
| --- | --- | --- | --- | --- | --- | --- |
| +0.8774 | +33.84% | 26.63% | 63.2% | +1.10 | 57 | 22.7% |

**诊断（登记用，非 HL 轮次）**：全窗口 IR 为正但未达部署级显著性（t < 2）；holdout
优于样本内（分差 −0.55），无「样本内虚高」迹象，但 holdout 仅 1.6 年、IR 方差大。
超额最大回撤 60% 偏高（Top3 集中 + 动量尾部风险）。后续 HL 轮次以 **IR > 0.4436**
为硬门槛，合 main 须 additionally t ≥ 2 + holdout IR > 0。

> **复现注（2026-07-05 晚）**：同一 `panel_csi300_*.pkl` 在 worktree 复跑得 IR
> **+0.8676**（非上文 +0.4436）。疑 partial 混拉阶段数值已变；**HL 轮次以 worktree
> 实测 +0.8676 为比较基线**，待腾讯前复权重拉面板后重锚。

## 横截面 HL 轮次

## ⚠️ evaluator v3 对齐（2026-07-05 夜）

为对齐聚宽 `jq_strategy_xs_export.py`，本地 `backtest_xs.py` 在本轮做了**结构性口径修正**：

1. **月初时点对齐**：从“月初收盘算分、次日开盘成交”改为**前一交易日收盘算分、
   月初首个交易日开盘成交**，对齐聚宽 `run_monthly(..., time='open') + previous_date`。
2. **point-in-time 股票池**：从 2026 固定快照改为 `index-constitution` 历史成分，
   每次调仓按日期过滤当期真实 CSI300 成分。
3. **非科创板约束**：按用户实盘约束排除 `688xxx`，本地 evaluator 与聚宽导出同步。

> 这意味着下方基于旧 evaluator 的截面轮次结论，只能视为**旧口径历史记录**；
> 新一轮 HL 必须以 v3 evaluator 重新比较。

## evaluator v4（科技50 + Top5，2026-07-06）

在 v3（全 CSI300 非科创板 + Top3）上连续多轮 HL 无法得到可接受候选后，重新设定固定
评估器，目标是降低个股噪声与风格异质性：

1. **股票池缩窄**：改为 **科技50 固定白名单 ∩ 当期 CSI300 历史真实成分**；
2. **持仓数提升**：`Top 3 → Top 5`，降低单票特异性风险；
3. 其余口径保持：前一交易日收盘算分、月初开盘成交、point-in-time 成分、非科创板。

### v4 基线锚点（纯 12-1 动量）

| 指标 | 全窗口 | 样本内 | holdout |
| --- | --- | --- | --- |
| IR | **+0.5668** | +0.6920 | **−0.0584** |
| 年化超额 | +17.27% | +21.24% | −1.71% |
| 超额最大回撤 | 38.30% | 34.48% | 38.30% |
| 月度胜率 | 53.6% | 57.7% | 42.1% |
| t 统计量 | +1.59 | +1.73 | −0.07 |
| 年换手（单边） | 6.88× | 6.74× | 7.36× |
| 有效持仓次数 | 425 | 330 | 95 |
| 单点最大贡献 | 2.6% | 3.1% | 11.4% |

**诊断**：相对 v3，v4 的 holdout 已从 **−0.5102** 明显收敛到 **−0.0584**，说明
“缩池 + 提升持仓数”是有效方向；但样本外仍未转正，因此新一轮 HL 继续以：

- 全窗口 IR **严格 > 0.5668**
- holdout IR **≥ 0**
- 样本内−holdout **≤ 1.0**

作为硬门槛。

### XS Round 1（v4 基线）— 12-1 动量 + 长期趋势软确认 · REJECT

- **Probe（v4 基线）**：全窗口 IR **+0.5668**，holdout IR **−0.0584**，样本内−holdout
  **+0.7504**，超额最大回撤 38.30%，年换手 6.88×。
- **Diagnose**：v4 基线样本外已接近打平，说明“科技50 + Top5”大幅降低了噪声；
  但 holdout 仍略负，推测仍有部分“长周期动量在当前已跌回长期趋势线下”的票拖累表现。
- **Propose**：在 12-1 动量上做**长期趋势软确认**：用 `price / MA200` 给动量加权，而不是
  像旧轮次那样做硬过滤。经济含义：仍在长期上升结构里的动量更可持续，破位票应降权。
- **Patch**：`strategy_xs.py` 新增 `TREND_WINDOW=200`，`score = mom_12 * (price / ma200)`。
- **Evaluate（候选）**：

  | 指标 | v4 基线 | 候选 | 门槛 | 判定 |
  | --- | --- | --- | --- | --- |
  | 全窗口 IR | **+0.5668** | +0.5918 | 严格 > 基线 | ✅ |
  | holdout IR | **−0.0584** | −0.3399 | ≥ 0 | ❌ |
  | 样本内−holdout | +0.7504 | +1.1293 | ≤ 1.0 | ❌ |
  | 年换手 | 6.88× | 5.92× | 报告项 | 改善 |
  | 单点贡献 | 2.6% | 2.3% | ≤ 30% | ✅ |

  补充观察：全窗口略升、换手略降，但 holdout 从接近打平退化为显著负值。
- **Decide**：**REJECT**。虽然全窗口 IR 小幅提高，但样本外再度转弱，且样本内外分化进入
  拒绝区，不能接受。
- **Compress**：已回滚 `strategy_xs.py` 到 v4 基线（纯 12-1 动量）。

### XS Round 2（v4 基线）— 12-1 动量 + 月度持续性确认 · REJECT

- **Probe（v4 基线）**：全窗口 IR **+0.5668**，holdout IR **−0.0584**，样本内−holdout
  **+0.7504**，超额最大回撤 38.30%，年换手 6.88×。
- **Diagnose**：v4 基线样本外只差临门一脚，但仍略负。怀疑部分票的年度动量来自少数几个月
  的急涨，而不是更连续的月度上行，这类“脉冲型”趋势在后续 holdout 里更容易失效。
- **Propose**：保留 12-1 主动量，用最近 3 个月“月度正收益占比”做软加权。
  经济含义：若多数月份都能维持上涨，则趋势连续性更强，排序应适度上调。
- **Patch**：`score = mom_12 * (0.5 + 最近3个21日窗口正收益占比)`。
- **Evaluate（候选）**：

  | 指标 | v4 基线 | 候选 | 门槛 | 判定 |
  | --- | --- | --- | --- | --- |
  | 全窗口 IR | **+0.5668** | +0.6385 | 严格 > 基线 | ✅ |
  | holdout IR | **−0.0584** | −0.5371 | ≥ 0 | ❌ |
  | 样本内−holdout | +0.7504 | +1.4209 | ≤ 1.0 | ❌ |
  | 超额最大回撤 | 38.30% | 47.88% | 报告项 | 退化 |
  | 年换手 | 6.88× | 7.14× | 报告项 | 略差 |
  | 单点贡献 | 2.6% | 2.5% | ≤ 30% | ✅ |

- **Decide**：**REJECT**。候选虽然提高了全窗口 IR，但样本外明显恶化，样本内外分化进入
  拒绝区，不可接受。
- **Compress**：已回滚 `strategy_xs.py` 到 v4 基线（纯 12-1 动量）。

## 多因子 v1 入门基线（探索根，不并入 v4 动量主线）

用户明确要求引入 `JQ_strategy_1.py` 的“多因子思想”，但不照搬其参数。因此在当前
`科技50 + Top5 + point-in-time CSI300 ∩ 非科创板` evaluator 上，增加一条**探索分支基线**：

- **质量**：当前快照 `ROE`（EastMoney `f173`）
- **估值**：当前快照 `EP = 1 / PE`（`PE` 用 EastMoney `f162` 近似）
- **动量**：`12-1` 价格动量（跳过近 1 个月）
- **合成方式**：`rank(ROE) + rank(EP) + rank(MOM_12_1)` 等权相加

### 多因子 v1 评估（2026-07-06）

| 指标 | 全窗口 | 样本内 | holdout |
| --- | --- | --- | --- |
| IR | **+0.7635** | +0.9618 | **−0.2422** |
| 年化超额 | +20.82% | +26.98% | −5.75% |
| 超额最大回撤 | 42.55% | 32.16% | 35.37% |
| 月度胜率 | 55.7% | 57.7% | 52.6% |
| t 统计量 | +2.14 | +2.41 | −0.30 |
| 年换手（单边） | 4.05× | 4.13× | 3.78× |
| 有效持仓次数 | 425 | 330 | 95 |
| 单点最大贡献 | 4.6% | 5.5% | 11.5% |

**解读**：
- 相比 v4 纯动量基线（IR `0.5668`，holdout IR `-0.0584`），多因子 v1 的
  **全窗口 IR 明显更高**、换手更低、回撤略高；
- 但 holdout 仍为 **负值**，且样本内−holdout = `1.2040`，进入拒绝区。

**定位**：这版不是“接受候选”，而是后续挖掘新因子的**探索根**。如果你后续明确转向
“多因子路线”，我们应以它为新的起点继续因子挖掘；如果继续沿“价格动量路线”，则仍以
v4 纯动量为主线基线。

### XS Round 1 — 风险调整动量（mom/vol_63d）· REJECT

- **Probe（v0 基线，同缓存复跑）**：IR **+0.8676**，年化超额 +62.07%，超额最大回撤
  81.54%，t +2.43，单点 3.6%；holdout IR +0.1657，样本内−holdout **+0.8112**。
- **Diagnose**：纯 12-1 动量 IR 高但 holdout 远弱于样本内（近 1.0 拒绝线），超额回撤
  81% 不可接受；Top3 易选高波动「彩票型」动量赢家。
- **Propose**：12-1 动量除以近 63 日日收益波动率 → 偏好涨得稳的标的（风险调整动量）。
- **Patch**：`strategy_xs.py` 新增 `VOL_WINDOW=63`，`score = mom / vol`。
- **Evaluate（候选）**：

  | 指标 | 基线 | 候选 | 第一级门槛 | 判定 |
  | --- | --- | --- | --- | --- |
  | IR | +0.8676 | **+0.8236** | 严格 > 基线 | ❌ |
  | holdout IR | +0.1657 | **−0.4533** | ≥ 0 | ❌ |
  | 样本内−holdout | +0.8112 | **+1.5144** | ≤ 1.0 | ❌ |
  | 单点贡献（全窗口） | 3.6% | 4.5% | ≤ 30% | ✅ |
  | 有效持仓 | 255 | 255 | ≥ 100 | ✅ |

  副作用：超额最大回撤 81%→44%（改善），但 score 与 holdout 双杀。
- **Decide**：**REJECT**。IR 未超基线；holdout 转负；样本内外分化超 1.0（过拟合区）。
- **Compress**：已回滚 `strategy_xs.py` 至 v0 纯动量。

### XS Round 2 — 200 日趋势过滤动量 · ACCEPT

- **Probe（Round 1 后基线，同缓存复跑）**：IR **+0.8676**，年化超额 +62.07%，超额最大回撤
  81.54%，t +2.43，单点 3.6%；holdout IR +0.1657，样本内−holdout **+0.8112**。
- **Diagnose**：纯 12-1 动量虽有正 holdout，但样本内明显强于样本外，且超额回撤高达 81%。
  说明 Top3 常把“过去涨过、但已转入下行趋势”的高弹性个股继续排进组合。
- **Propose**：对个股加 **200 日趋势过滤**，只给仍站在长期均线之上的股票打分。
  经济含义：保留截面动量，剔除已破坏长期上升结构的候选，减少下行反弹/趋势反转股。
- **Patch**：`strategy_xs.py` 新增 `TREND_WINDOW=200`；`price <= ma_trend` 时直接过滤，
  其余股票仍按 12-1 动量排序。
- **Evaluate（候选）**：

  | 指标 | 基线 | 候选 | 第一级/第二级门槛 | 判定 |
  | --- | --- | --- | --- | --- |
  | IR | +0.8676 | **+1.0283** | 严格 > 基线 | ✅ |
  | holdout IR | +0.1657 | **+0.2909** | ≥ 0 | ✅ |
  | 样本内−holdout | +0.8112 | **+0.8948** | ≤ 1.0 | ✅ |
  | 单点贡献（全窗口） | 3.6% | 4.7% | ≤ 30% | ✅ |
  | 单点贡献（holdout） | 8.9% | 26.6% | ≤ 30% | ✅ |
  | 有效持仓 | 255 | 255 | ≥ 100 | ✅ |
  | 全窗口 t | +2.43 | **+2.88** | ≥ 2 | ✅ |

  补充观察：超额最大回撤 **81.54% → 50.10%**，holdout 年化超额 **+5.71% → +9.95%**。
- **Decide**：**ACCEPT**。候选同时通过第一级与第二级门槛：IR 严格高于基线、holdout 为正、
  样本内外分化未进拒绝区、全窗口 t≥2，且回撤显著下降。
- **Compress**：保留“12-1 动量 + 200 日趋势过滤”这一最小结构，不叠加额外连续变量调参。

### XS Round 2（在 evaluator v3 下复评）— REJECT

- **聚宽实测（用户截图，2024-07-09 ~ 2026-02-27）**：
  策略收益 **−8.91%**，基准收益 **+38.48%**，超额收益 **−34.22%**。
- **本地复评（v3：时点对齐 + PIT + 非科创板）**：

  | 口径 | 策略总收益 | 基准总收益 | 超额总收益 |
  | --- | --- | --- | --- |
  | 本地 holdout | **−6.34%** | **+36.95%** | **−43.29%** |
  | 聚宽 holdout | **−8.91%** | **+38.48%** | **−34.22%** |

  方向与量级已基本一致：本地与聚宽都显示**显著跑输基准**。

- **在 v3 evaluator 下重算指标**（当前 `strategy_xs.py` 已回滚到 v0，以下仅复评该候选）：

  | 指标 | v0 基线（v3） | Round 2 候选（v3） | 门槛 | 判定 |
  | --- | --- | --- | --- | --- |
  | 全窗口 IR | **+0.5029** | +0.3713 | 严格 > 基线 | ❌ |
  | holdout IR | **−0.5102** | −0.5118 | ≥ 0 | ❌ |
  | 样本内−holdout | +1.2522 | +1.0944 | ≤ 1.0 | ❌ |
  | 全窗口 t | +1.41 | +1.04 | ≥ 2 | ❌ |

- **Decide**：**REJECT**。Round 2 在对齐后的 evaluator 下不再成立，且聚宽终验与本地
  v3 holdout 都给出同方向负面结论。
- **Compress**：`strategy_xs.py` 已回滚到 **v0 纯 12-1 动量**；后续 HL 从 v3 基线重开。

### XS Round 1（v3 基线重开）— 6-1 动量 · REJECT

- **Probe（v3 基线 / v0 纯 12-1 动量）**：全窗口 IR **+0.5029**，holdout IR **−0.5102**，
  样本内−holdout **+1.2522**，超额最大回撤 56.36%。
- **Diagnose**：v3 基线在 2024-07~2026-02 的样本外已经显著失效，说明 12-1 动量过慢，
  可能跟不上近两年大盘风格切换与领涨主线轮动。
- **Propose**：把动量窗口从 12-1 缩短为 **6-1**。经济含义：在轮动更快的 A 股大盘股里，
  中期动量可能比年度动量更能捕捉当下主线，同时保留跳过近 1 个月来规避短反转。
- **Patch**：`LOOKBACK 252 → 126`，其余逻辑不变。
- **Evaluate（候选）**：

  | 指标 | v3 基线 | 6-1 候选 | 门槛 | 判定 |
  | --- | --- | --- | --- | --- |
  | 全窗口 IR | **+0.5029** | +0.0677 | 严格 > 基线 | ❌ |
  | holdout IR | **−0.5102** | −0.5595 | ≥ 0 | ❌ |
  | 样本内−holdout | +1.2522 | +0.6519 | ≤ 1.0 | ✅ |
  | 有效持仓 | 255 | 273 | ≥ 100 | ✅ |
  | 单点贡献 | 4.8% | 7.9% | ≤ 30% | ✅ |

  副作用：超额最大回撤 **56.36% → 130.31%**，年换手 **9.89× → 13.98×**，风险显著恶化。
- **Decide**：**REJECT**。6-1 动量既没有提高 score，也没有修复样本外失效，反而把回撤和换手
  明显放大。
- **Compress**：已回滚 `strategy_xs.py` 到 v3 基线（v0 纯 12-1 动量）。
